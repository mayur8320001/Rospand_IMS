@model IEnumerable<Rospand_IMS.Models.Employee.Department>

@{
    ViewData["Title"] = "Departments";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3">Departments</h1>
        <a asp-action="CreateDepartment" class="btn btn-primary">
            <i class="fas fa-plus"></i> Add New Department
        </a>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            @TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="card">
        <div class="card-body">
            @if (Model.Any())
            {
                <div class="table-responsive">
                    <table class="table table-striped table-hover">
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Description</th>
                                <th>Created Date</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var department in Model)
                            {
                                <tr>
                                    <td>@department.Name</td>
                                    <td>@department.Description</td>
                                    <td>@department.CreatedDate.ToString("dd/MM/yyyy")</td>
                                    <td>
                                        <a asp-action="EditDepartment" asp-route-id="@department.Id" class="btn btn-sm btn-outline-primary">Edit</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="text-center py-5">
                    <i class="fas fa-building fa-3x text-muted mb-3"></i>
                    <h4>No Departments Found</h4>
                    <p class="text-muted">Create your first department to get started.</p>
                    <a asp-action="CreateDepartment" class="btn btn-primary">Add New Department</a>
                </div>
            }
        </div>
    </div>
</div>